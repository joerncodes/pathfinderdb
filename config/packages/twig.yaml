twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        projectname: '%projectname%'
        subtitle: 'API access to the rules of the Pathfinder RPG by Paizo.'
        paypallink: '%paypallink%'
        githublink: '%githublink%'
        changelog:
            'Sept. 30, 2019':
                - 'added Rogue and Monk to the core classes. Somehow forgot those guys'
                - '`description` and `role` added for classes'
                - '`domain` and domain spells added'
                - 'Changelog added 🎉'
