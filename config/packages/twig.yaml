twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        projectname: '%projectname%'
        subtitle: 'API access to the rules of the Pathfinder RPG by Paizo.'
        paypallink: '%paypallink%'
